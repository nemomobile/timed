diff --git a/src/server/timed.cpp b/src/server/timed.cpp
index f215f1c..e46d4c9 100644
--- a/src/server/timed.cpp
+++ b/src/server/timed.cpp
@@ -74,8 +74,8 @@ Timed::Timed(int ac, char **av) : QCoreApplication(ac, av)
   spam() ;
   halted = "" ;
   //check acting dead / user mode
-  bool act_dead_mode = access("/tmp/ACT_DEAD", F_OK) == 0 ;
-  bool user_mode = access("/tmp/USER", F_OK) == 0 ;
+  bool act_dead_mode = false ;
+  bool user_mode = true ;
 
   if(act_dead_mode == user_mode)
   {
diff --git a/src/tests.xml b/src/tests.xml
index b959507..20bdec5 100644
--- a/src/tests.xml
+++ b/src/tests.xml
@@ -12,7 +12,6 @@
         <step>ls -l /var/cache/timed/SYSTEM_TIME_LOST 2>&amp;1 | tee -a /tmp/environment</step>
         <step>uptime                            | tee -a /tmp/environment</step>
         <step>date                              | tee -a /tmp/environment</step>
-        <step>cat /tmp/STATE                    | tee -a /tmp/environment</step>
         <step>whoami                            | tee -a /tmp/environment</step>
         <step>set                               | tee -a /tmp/environment</step>
         <step>ps | grep timed | grep -v grep    | tee -a /tmp/environment</step>
